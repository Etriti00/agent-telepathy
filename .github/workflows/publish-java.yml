name: Publish Java SDK to Maven Central

on:
  workflow_dispatch: {}
  workflow_run:
    workflows: ["TPCP CI/CD"]
    types: [completed]

jobs:
  publish:
    runs-on: ubuntu-latest
    if: >
      github.event_name == 'workflow_dispatch' ||
      (github.event.workflow_run.conclusion == 'success' &&
      startsWith(github.event.workflow_run.head_branch, 'v'))
    steps:
      - uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          server-id: central
          server-username: MAVEN_USERNAME
          server-password: MAVEN_PASSWORD

      - name: Import GPG key
        run: echo "${{ secrets.GPG_PRIVATE_KEY }}" | gpg --batch --import

      - name: Test credentials against Central Portal API
        env:
          MAVEN_USERNAME: ${{ secrets.MAVEN_USERNAME }}
          MAVEN_PASSWORD: ${{ secrets.MAVEN_PASSWORD }}
        run: |
          ENCODED=$(echo -n "${MAVEN_USERNAME}:${MAVEN_PASSWORD}" | base64)
          echo "Testing with Bearer auth (base64 encoded token)..."
          curl -s -o /dev/null -w "HTTP Bearer Status: %{http_code}\n" \
            -H "Authorization: Bearer ${ENCODED}" \
            "https://central.sonatype.com/api/v1/publisher/published?namespace=io.github.etriti00"

      - name: Publish to Maven Central
        run: cd tpcp-java && mvn --batch-mode -e deploy
        env:
          MAVEN_USERNAME: ${{ secrets.MAVEN_USERNAME }}
          MAVEN_PASSWORD: ${{ secrets.MAVEN_PASSWORD }}
          MAVEN_GPG_PASSPHRASE: ${{ secrets.GPG_PASSPHRASE }}
