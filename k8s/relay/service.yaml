apiVersion: v1
kind: Service
metadata:
  name: tpcp-relay
  namespace: tpcp-system
  labels:
    app.kubernetes.io/name: tpcp-relay
    app.kubernetes.io/component: relay
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: tpcp-relay
  ports:
    - name: ws
      port: 8765
      targetPort: 8765
      protocol: TCP
