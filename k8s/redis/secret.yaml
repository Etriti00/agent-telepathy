apiVersion: v1
kind: Secret
metadata:
  name: redis-credentials
  namespace: tpcp-system
type: Opaque
data:
  # Base64-encoded password. Generate with: echo -n 'your-password' | base64
  # Fill this value before deploying to any environment.
  password: <base64-encoded-password>
