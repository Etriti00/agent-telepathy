# Aura TPCP Bridge dependencies
# The tpcp package is installed from the local agent-telepathy repo:
#   pip install -e ../../tpcp

pydantic>=2.5.0
pydantic[email]>=2.5.0
websockets>=12.0
cryptography>=42.0.0
aiosqlite>=0.20.0
